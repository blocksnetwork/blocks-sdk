# Copy this file to `.env` (already gitignored) and fill in your key.
# `blocks publish` from this directory writes BLOCKS_API_KEY into .env for you.
BLOCKS_API_KEY=

# Optional: point at a non-production Blocks instance.
# BLOCKS_CDM_URL=
