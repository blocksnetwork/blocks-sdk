# Copy this file to `.env` (already gitignored) and fill in your key.
# Use the SAME key as the provider so both authenticate as the same user.
BLOCKS_API_KEY=

# Optional overrides:
# AGENT_NAME=symmetry_provider
# DURATION_MIN=1
# BLOCKS_CDM_URL=
