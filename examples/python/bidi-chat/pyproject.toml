[project]
name = "bidi-chat-python"
version = "1.0.0"
description = "Bidirectional chat example for the Blocks Network Python SDK"
requires-python = ">=3.10"
dependencies = [
  "blocks-network",
  "python-dotenv>=1.0.0",
]

[tool.setuptools]
py-modules = ["handler", "main"]
